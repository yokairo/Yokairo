{
  "name": "yokai-cinematic-ui",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "python3 -m http.server 4173 --bind 0.0.0.0",
    "check": "node --check app.js && node scripts/validate-site.mjs",
    "build": "npm run check"
  }
}
